export default class ProdutoImposto {
  constructor(gac, gap, d10, oleoDisel) {
    this.gac = Number(gac);
    this.gap = Number(gap);
    this.d10 = Number(d10);
    this.oleoDisel = Number(oleoDisel);
  }
}
